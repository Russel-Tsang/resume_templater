<Resume>
    <ContactBlock>
        <Name />
        <ContactInfo />
    </ContactBlock>
    <Skills />
    <GenericSection> 
        project={!!project} 
        headTitle={headTitle} 
        itemTitle={itemTitle} 
        itemCaption={itemCaption}
        date={date}
        bulletPoints={bulletPoints}
    >
        <GenericSectionTitle headTitle={props.headTitle} />
        <GenericItemTitle itemTitle={props.itemTitle} project={props.project} date={props.date}/>
        <GenericItemCaption itemCaption={props.itemCaption} />
        {/* props.bulletPoints.map( bulletPoint => <BulletPoint text={bulletPoint} > ) */}
    </GenericSection>
</Resume>